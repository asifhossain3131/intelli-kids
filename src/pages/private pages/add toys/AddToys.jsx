import React from 'react';
import useTitle from '../../../hooks/useTitle';

const AddToys = () => {
    useTitle('Add Toys')
    return (
        <div>
            
        </div>
    );
};

export default AddToys;